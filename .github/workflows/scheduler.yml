name: Scheduler
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger scheduler
        run: curl -s -X POST ${{ secrets.APP_URL }}/api/jobs/tick
